# UAT run records

One file per completed acceptance run, named for the version tested
(`v1.3.0-rc1.md`). Use the template at the bottom of [`../../UAT.md`](../../UAT.md).

These exist so that "was this build ever tested against real instruments, and
which ones?" has an answer months later. A run that only lives in someone's
memory is not a run.
